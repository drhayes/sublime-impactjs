<snippet>
    <content><![CDATA[ig.module(
  'game.entities.${1:moduleName}'
)
.requires(
  'impact.entity'
)
.defines(function() {'use strict';
  Entity${1/^(.)/\U\1/} = ig.Entity.extend({
    ${2:init: function(x, y, settings) {
      this.parent(x, y, settings);
      $0
    }}
  });
});
]]></content>
    <tabTrigger>ige</tabTrigger>
    <scope>source.js.impact</scope>
    <description>
      Defines an Impact entity.
    </description>
</snippet>
